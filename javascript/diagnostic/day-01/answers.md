# Ответы: день 1

## Q1
1
2
10

<!-- 
В first передан 0 в параметр , соответственно value = 0, затем value = 1, value = 2.
В second передан 10 в параметр, соответственно value = 10 прочитано через функцию.

Чисто размышления, но не уверен, код в обоих случаях может вернуть undefined, т.к. функции завернуты в общем return. Возможно не прав.
 -->

## Q2
Будет в обоих случаях dark

dark
dark

так как перезапишет по ссылке.


Независимую копию в данном примере можно получить через JSON.stringify - parse.
Или structuredClone()

## Q3
var поменять на let

Что выведет исходный код с var, и объясните:
- когда завершится цикл; после 3 итерации
- когда выполнятся callback-функции таймеров; в самом конце когда выполнится весь синхронный код
- одна или несколько переменных index создаются при использовании var. при var будет одна переменная index

## Q4
A
E
C
D
B

Сначала все синхронные вызовы сверху вниз.
После приоритетная очередь для микротаксков, к которым относят и промисы. Как очередь выполнится после нее переходит к макротакс, которым является setTimeout.

Не совсем уверен на счет очередности C D, в остальном так.

## Q5
Да прекратится, так как all нужен когда все промисы успешны. Для такой задачи подойдет Promise.allSettled.

Что произойдёт с промисом, возвращённым Promise.all. - перейдет в состояние rejected
Что произойдёт с уже запущенными асинхронными операциями. - продолжит выполняться, т.к. не умеет автоматически их отменять, можно использовать AbortController для их отмены.

## Q6
Не смогу ответить на данный вопрос.

const user = { name: "Alex" };
const previousUser = user;

user.name = "Max";
const nextUser = user;

Ответьте:
- Равны ли previousUser и nextUser через ===? равны.
- Создалась ли новая ссылка? нет
- Что увидит React, если сравнит предыдущее и следующее состояние по ссылке? не увидит изменений, чисто логически

