# Подготовка к собеседованию — день 1

Ответьте кратко, но своими словами. Для каждого ответа приведите небольшой пример.

1. Чем `const` отличается от неизменяемого значения? - const и есть неизменяемое значение.
2. Что такое замыкание и где оно полезно в реальном приложении? - замыкание это внутренняя функция помнит переменную своей родительской функции, полезно в случаях когда нам нужно инкапсулировать значение, чтобы извне нельзя было изменить значение переменной.

fubction a() {
    let x = 0;

    function b(){
        x += 1
    }
}
3. Чем отличаются `map`, `filter` и `reduce`? Когда `reduce` ухудшает читаемость? - все методы проходят по массиву, map когда нужно изменить каждое поле, фильтер когда нужно отфильтровать по полю, reduce работает как аккамулятор, условно когда нужно сложить все числа массива, читаемость ухудщает сложные конструкции внутри.
4. Что возвращает `async`-функция и как обрабатываются её ошибки? - возвращет промис, ошибки обрабатываются через reject.
5. Почему иммутабельные обновления важны для React и Redux? - не знаю...
